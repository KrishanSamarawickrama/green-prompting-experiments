Guidance for tagged/explained prompts:
- Keep explanations internal; emit only the final Python code block.
- Ensure a callable function `run_task(...)` exists and is deterministic with a fixed seed.
